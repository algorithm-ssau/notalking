# v0.2.1

## Changes

- Replaced the assistant provider cycle toggle with a real dropdown that lists the configured provider options plus the default Ollama target.
- Added live MCP call tracing in the assistant panel so each request shows which Core-backed methods were called while the answer is being prepared.
- Pinned the assistant composer to the bottom of the panel and kept the panel body stretched correctly across responsive layouts.
- Swapped the send icon to an up-arrow from Tabler Icons.
- Added a Settings -> Assistant super prompt section that is injected into every LLM request after being saved locally.
